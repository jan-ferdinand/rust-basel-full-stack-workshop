[package]
name = "frontend"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { version = "0.5.1", features = ["web"] }
manganis = "0.2.2"
model = { version = "0.1.0", path = "../model" }
reqwest = { version = "0.12.4", features = ["json"] }
